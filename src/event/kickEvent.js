function setupKickEvent (instance) {
    //instance.on('kicked', (reason) => settings.chatSigning = reason.includes("unsigned_chat") ? false : settings.chatSigning);

}

module.exports = { setupKickEvent }

